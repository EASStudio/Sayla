// ==== C source: personal coding LLM project ====

// Module includes
#include "BPE.h"

// Helpers:

// Scans the whole linked-list sequence, tallies every adjacent pair, and
// returns the most frequent one. Returns 0 if nothing appears more than once
// (i.e. no merge is worth making).
static int findBestPair(TokenNode* head, int* outA, int* outB)
{
    static PairCount pairs[MAX_PAIR_TRACK];
    int numPairs = 0;
 
    TokenNode* cur = head;
    while (cur != NULL && cur->next != NULL)
    {
        int a = cur->id;
        int b = cur->next->id;
 
        int found = -1;
        for (int i = 0; i < numPairs; i++)
        {
            if (pairs[i].a == a && pairs[i].b == b)
            {
                found = i;
                break;
            }
        }
 
        if (found >= 0)
        {
            pairs[found].count++;
        }
        else if (numPairs < MAX_PAIR_TRACK)
        {
            pairs[numPairs].a = a;
            pairs[numPairs].b = b;
            pairs[numPairs].count = 1;
            numPairs++;
        }
 
        cur = cur->next;
    }
 
    if (numPairs == 0) return 0;
 
    int bestIdx = 0;
    for (int i = 1; i < numPairs; i++)
    {
        if (pairs[i].count > pairs[bestIdx].count)
        {
            bestIdx = i;
        }
    }
 
    if (pairs[bestIdx].count < 2) return 0; // merging a one-off pair isn't useful
 
    *outA = pairs[bestIdx].a;
    *outB = pairs[bestIdx].b;
    return pairs[bestIdx].count;
}
 
// Replaces every occurrence of the adjacent pair (a, b) in the list with a
// single node carrying newId.
static void applyMergeToList(TokenNode* head, int a, int b, int newId)
{
    TokenNode* cur = head;
    while (cur != NULL && cur->next != NULL)
    {
        if (cur->id == a && cur->next->id == b)
        {
            TokenNode* nodeB = cur->next;
            cur->id = newId;
            cur->next = nodeB->next;
 
            if (nodeB->next != NULL)
            {
                nodeB->next->prev = cur;
            }
 
            free(nodeB);
        }
 
        cur = cur->next;
    }
}

void initTokenizer(Tokenizer* t)
{
    t->vocabSize = 256;
    t->numMerges = 0;
 
    // Base vocab: every single byte value is its own token
    for (int i = 0; i < 256; i++)
    {
        t->vocabTable[i] = (char*)malloc(2);
        t->vocabTable[i][0] = (char)i;
        t->vocabTable[i][1] = '\0';
    }
}

void trainBPE(Tokenizer* t, const char* corpus, int targetVocabSize)
{
    initTokenizer(t);
 
    int len = strlen(corpus);
    if (len == 0) return;
 
    // Build the initial byte-level linked list for the whole corpus
    TokenNode* head = (TokenNode*)malloc(sizeof(TokenNode));
    head->id = (unsigned char)corpus[0];
    head->prev = NULL;
    head->next = NULL;
 
    TokenNode* current = head;
    for (int i = 1; i < len; i++)
    {
        TokenNode* newNode = (TokenNode*)malloc(sizeof(TokenNode));
        newNode->id = (unsigned char)corpus[i];
        newNode->prev = current;
        newNode->next = NULL;
        current->next = newNode;
        current = newNode;
    }
 
    while (t->vocabSize < targetVocabSize && t->numMerges < MAX_VOCAB_SIZE)
    {
        int a, b;
        int count = findBestPair(head, &a, &b);
        if (count == 0) break; // nothing left worth merging
 
        int newId = 256 + t->numMerges;
 
        t->merges[t->numMerges].tokenA = a;
        t->merges[t->numMerges].tokenB = b;
        t->numMerges++;
 
        // The new token decodes to whatever its two parts decode to, concatenated
        int lenA = strlen(t->vocabTable[a]);
        int lenB = strlen(t->vocabTable[b]);
        t->vocabTable[newId] = (char*)malloc(lenA + lenB + 1);
        memcpy(t->vocabTable[newId], t->vocabTable[a], lenA);
        memcpy(t->vocabTable[newId] + lenA, t->vocabTable[b], lenB);
        t->vocabTable[newId][lenA + lenB] = '\0';
 
        t->vocabSize++;
 
        applyMergeToList(head, a, b, newId);
    }
 
    current = head;
    while (current != NULL)
    {
        TokenNode* temp = current;
        current = current->next;
        free(temp);
    }
}

int getMergeRank(Tokenizer* t, int idA, int idB) 
{
    for (int i = 0; i < t->numMerges; i++) 
    {
        if (t->merges[i].tokenA == idA && t->merges[i].tokenB == idB) 
        {
            return i;            
        }
    }

    return -1;
}

int tokenizeEncode(Tokenizer* t, const char* text, int* outputTokens)
{
    int len = strlen(text);
    if (len == 0) return 0;
 
    TokenNode* head = (TokenNode*)malloc(sizeof(TokenNode));
    head->id = (unsigned char)text[0];
    head->prev = NULL;
    head->next = NULL;
 
    TokenNode* current = head;
    for (int i = 1; i < len; i++)
    {
        TokenNode* newNode = (TokenNode*)malloc(sizeof(TokenNode));
        newNode->id = (unsigned char)text[i];
        newNode->prev = current;
        newNode->next = NULL;
        current->next = newNode;
        current = newNode;
    }
 
    while(1)
    {
        TokenNode* bestNode = NULL;
        int bestRank = INF;
 
        current = head;
        while (current != NULL && current->next != NULL)
        {
            int rank = getMergeRank(t, current->id, current->next->id);
 
            if (rank != -1 && rank < bestRank)
            {
                bestRank = rank;
                bestNode = current;
            }
 
            current = current->next;
        }
 
        if (bestNode == NULL)
            break;
 
        int mergedId = 256 + bestRank;
 
        TokenNode* nodeA = bestNode;
        TokenNode* nodeB = bestNode->next;
 
        nodeA->id = mergedId;
        nodeA->next = nodeB->next;
 
        if (nodeB->next != NULL)
        {
            nodeB->next->prev = nodeA;
        }
 
        free(nodeB);
    }
 
    int tokenCount = 0;
    current = head;
 
    while (current != NULL)
    {
        outputTokens[tokenCount++] = current->id;
        TokenNode* temp = current;
        current = current->next;
        free(temp);
    }
 
    return tokenCount;
}
 
int tokenizeDecode(Tokenizer* t, const int* tokens, int tokenCount, char* output, int maxLen)
{
    int pos = 0;
 
    for (int i = 0; i < tokenCount; i++)
    {
        int id = tokens[i];
        if (id < 0 || id >= t->vocabSize || t->vocabTable[id] == NULL) continue;
 
        int pieceLen = strlen(t->vocabTable[id]);
        if (pos + pieceLen >= maxLen) break; // avoid overflowing the output buffer
 
        memcpy(output + pos, t->vocabTable[id], pieceLen);
        pos += pieceLen;
    }
 
    output[pos] = '\0';
    return pos;
}
 
void freeTokenizer(Tokenizer* t)
{
    for (int i = 0; i < t->vocabSize; i++)
    {
        free(t->vocabTable[i]);
        t->vocabTable[i] = NULL;
    }
 
    t->vocabSize = 0;
    t->numMerges = 0;
}
#pragma once
 
// Core includes
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_VOCAB_SIZE 50257
#define MAX_PAIR_TRACK 20000
#define INF 99999999

// BPE murge rule
typedef struct 
{
    int tokenA;
    int tokenB;
}MergeRule;

// Tokenizer state 
typedef struct 
{
    int vocabSize;
    char* vocabTable[MAX_VOCAB_SIZE];
    MergeRule merges[MAX_VOCAB_SIZE];
    int numMerges;
}Tokenizer;

// Node for a dynamic linked list representing the text sequence during merging
typedef struct TokenNode 
{
    int id;
    struct TokenNode* next;
    struct TokenNode* prev;
} TokenNode;

// One (pair, frequency) observation while scanning the corpus for merge candidates
typedef struct
{
    int a;
    int b;
    int count;
} PairCount;

void initTokenizer(Tokenizer* t);
void trainBPE(Tokenizer* t, const char* corpus, int targetVocabSize);
int getMergeRank(Tokenizer* t, int idA, int idB);
int tokenizeEncode(Tokenizer* t, const char* text, int* ouputTokens);
int tokenizeDecode(Tokenizer* t, const int* tokens, int tokenCount, char* ouput, int maxLen);
void freeTokenizer(Tokenizer* t);
// Module includes
#include "cache.h"

// Core includes
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// This format is NOT portable across machines with different endianness
// or double representations -- fine for a single-machine local cache, not
// meant to be shared between different computers.

static bool writeAll(FILE* f, const void* data, size_t size)
{
    return fwrite(data, 1, size, f) == size;
}

static bool readAll(FILE* f, void* data, size_t size)
{
    return fread(data, 1, size, f) == size;
}

bool saveModelCache(const char* path, const char* corpus, Tokenizer* t, EmbeddingTable* e, Network* net)
{
    FILE* f = fopen(path, "wb");
    if (!f) return false;

    int magic = CACHE_MAGIC;
    int corpusLen = (int)strlen(corpus);

    bool ok = true;
    ok = ok && writeAll(f, &magic, sizeof(magic));
    ok = ok && writeAll(f, &corpusLen, sizeof(corpusLen));
    ok = ok && writeAll(f, corpus, (size_t)corpusLen);

    // ---- Tokenizer ----
    ok = ok && writeAll(f, &t->vocabSize, sizeof(t->vocabSize));
    ok = ok && writeAll(f, &t->numMerges, sizeof(t->numMerges));

    for (int i = 0; i < t->numMerges && ok; i++)
        ok = ok && writeAll(f, &t->merges[i], sizeof(MergeRule));

    for (int i = 0; i < t->vocabSize && ok; i++)
    {
        int len = (int)strlen(t->vocabTable[i]);
        ok = ok && writeAll(f, &len, sizeof(len));
        ok = ok && writeAll(f, t->vocabTable[i], (size_t)len);
    }

    // ---- Embeddings ----
    ok = ok && writeAll(f, &e->vocabSize, sizeof(e->vocabSize));
    ok = ok && writeAll(f, &e->embeddingDim, sizeof(e->embeddingDim));

    for (int i = 0; i < e->vocabSize && ok; i++)
        ok = ok && writeAll(f, e->vectors[i], sizeof(double) * (size_t)e->embeddingDim);

    // ---- Network ----
    ok = ok && writeAll(f, &net->numLayers, sizeof(net->numLayers));

    for (int l = 0; l < net->numLayers && ok; l++)
    {
        Layer* layer = &net->layers[l];
        ok = ok && writeAll(f, &layer->numNeurons, sizeof(layer->numNeurons));
        ok = ok && writeAll(f, &layer->numInputs, sizeof(layer->numInputs));

        double lr = (layer->numNeurons > 0) ? layer->neurons[0].learningRate : 0.0;
        ok = ok && writeAll(f, &lr, sizeof(lr));

        for (int n = 0; n < layer->numNeurons && ok; n++)
        {
            Neuron* neuron = &layer->neurons[n];
            ok = ok && writeAll(f, &neuron->bias, sizeof(double));
            ok = ok && writeAll(f, neuron->weights, sizeof(double) * (size_t)layer->numInputs);
        }
    }

    fclose(f);

    if (!ok) remove(path); // don't leave a half-written cache file behind

    return ok;
}

bool loadModelCache(const char* path, const char* corpus, Tokenizer* t, EmbeddingTable* e, Network* net)
{
    FILE* f = fopen(path, "rb");
    if (!f) return false;

    bool ok = true;

    int magic = 0;
    ok = ok && readAll(f, &magic, sizeof(magic));
    if (!ok || magic != CACHE_MAGIC) { fclose(f); return false; }

    int corpusLen = 0;
    ok = ok && readAll(f, &corpusLen, sizeof(corpusLen));
    if (!ok || corpusLen != (int)strlen(corpus)) { fclose(f); return false; }

    char* storedCorpus = (char*)malloc((size_t)corpusLen + 1);
    ok = ok && readAll(f, storedCorpus, (size_t)corpusLen);
    storedCorpus[corpusLen] = '\0';
    bool corpusMatches = ok && (memcmp(storedCorpus, corpus, (size_t)corpusLen) == 0);
    free(storedCorpus);

    if (!corpusMatches) { fclose(f); return false; }

    // ---- Tokenizer ----
    ok = ok && readAll(f, &t->vocabSize, sizeof(t->vocabSize));
    ok = ok && readAll(f, &t->numMerges, sizeof(t->numMerges));

    for (int i = 0; i < t->numMerges && ok; i++)
        ok = ok && readAll(f, &t->merges[i], sizeof(MergeRule));

    for (int i = 0; i < t->vocabSize && ok; i++)
    {
        int len = 0;
        ok = ok && readAll(f, &len, sizeof(len));
        if (!ok) break;

        t->vocabTable[i] = (char*)malloc((size_t)len + 1);
        ok = ok && readAll(f, t->vocabTable[i], (size_t)len);
        if (ok) t->vocabTable[i][len] = '\0';
    }

    // Known limitation: if this fails partway through, whatever was
    // already malloc'd above (vocabTable entries, embedding vectors,
    // network layers below) is left allocated but unused -- the caller's
    // fallback path re-inits everything through fresh pointers rather than
    // reusing these. A leak on a rare corrupted-cache path, not a crash;
    // acceptable for a single-run desktop tool.
    if (!ok) { fclose(f); return false; }

    // ---- Embeddings ----
    ok = ok && readAll(f, &e->vocabSize, sizeof(e->vocabSize));
    ok = ok && readAll(f, &e->embeddingDim, sizeof(e->embeddingDim));

    if (ok)
    {
        e->vectors = (double**)malloc(sizeof(double*) * (size_t)e->vocabSize);
        for (int i = 0; i < e->vocabSize && ok; i++)
        {
            e->vectors[i] = (double*)malloc(sizeof(double) * (size_t)e->embeddingDim);
            ok = ok && readAll(f, e->vectors[i], sizeof(double) * (size_t)e->embeddingDim);
        }
    }

    if (!ok) { fclose(f); return false; }

    // ---- Network ----
    ok = ok && readAll(f, &net->numLayers, sizeof(net->numLayers));

    if (ok)
    {
        net->layers = (Layer*)malloc(sizeof(Layer) * (size_t)net->numLayers);

        for (int l = 0; l < net->numLayers && ok; l++)
        {
            Layer* layer = &net->layers[l];
            ok = ok && readAll(f, &layer->numNeurons, sizeof(layer->numNeurons));
            ok = ok && readAll(f, &layer->numInputs, sizeof(layer->numInputs));
            if (!ok) break;

            // Same rule initNetwork uses: only the final layer is softmax.
            // Deriving this from position rather than storing it keeps the
            // cache file format unchanged.
            layer->isSoftmax = (l == net->numLayers - 1);

            double lr = 0.0;
            ok = ok && readAll(f, &lr, sizeof(lr));

            layer->neurons = (Neuron*)malloc(sizeof(Neuron) * (size_t)layer->numNeurons);
            layer->outputs = (double*)malloc(sizeof(double) * (size_t)layer->numNeurons);
            layer->deltas  = (double*)malloc(sizeof(double) * (size_t)layer->numNeurons);

            for (int n = 0; n < layer->numNeurons && ok; n++)
            {
                Neuron* neuron = &layer->neurons[n];
                neuron->numInputs = layer->numInputs;
                neuron->learningRate = lr;

                ok = ok && readAll(f, &neuron->bias, sizeof(double));
                neuron->weights = (double*)malloc(sizeof(double) * (size_t)layer->numInputs);
                ok = ok && readAll(f, neuron->weights, sizeof(double) * (size_t)layer->numInputs);
            }
        }
    }

    fclose(f);
    return ok;
}
#pragma once

// Module includes
#include "BPE.h"
#include "embedding.h"
#include "network.h"

// Core includes
#include <stdbool.h>

// Arbitrary marker so we don't try to parse an unrelated file as a cache,
// and so a future incompatible format change can be detected (bump this).
#define CACHE_MAGIC 0x53415931

// Attempts to load a previously-trained tokenizer, embedding table, and
// network from disk, but only if the corpus stored alongside them exactly
// matches `corpus`. This means editing the training corpus automatically
// invalidates the cache and forces a fresh training run rather than
// silently loading a model trained on stale data.

bool loadModelCache(const char* path, const char* corpus, Tokenizer* t, EmbeddingTable* e, Network* net);
bool saveModelCache(const char* path, const char* corpus, Tokenizer* t, EmbeddingTable* e, Network* net);
// Module includes
#include "embedding.h"

// Core includes
#include <stdlib.h>

// Returns a small random double in [-0.1, 0.1] -- kept small so embeddings
// start close to zero, same rationale as the weight init in neuron.c.
static double randomEmbeddingValue(void)
{
    return ((double)rand() / (double)RAND_MAX) * 0.2 - 0.1;
}

void initEmbeddingTable(EmbeddingTable* e, int vocabSize, int embeddingDim)
{
    e->vocabSize = vocabSize;
    e->embeddingDim = embeddingDim;
    e->vectors = (double**)malloc(sizeof(double*) * vocabSize);

    for (int i = 0; i < vocabSize; i++)
    {
        e->vectors[i] = (double*)malloc(sizeof(double) * embeddingDim);
        for (int d = 0; d < embeddingDim; d++)
            e->vectors[i][d] = randomEmbeddingValue();
    }
}

const double* getEmbedding(EmbeddingTable* e, int tokenId)
{
    if (tokenId < 0 || tokenId >= e->vocabSize) tokenId = 0;
    return e->vectors[tokenId];
}

void updateEmbedding(EmbeddingTable* e, int tokenId, const double* gradient, double lr)
{
    if (tokenId < 0 || tokenId >= e->vocabSize) return;

    for (int d = 0; d < e->embeddingDim; d++)
        e->vectors[tokenId][d] += lr * gradient[d];
}

void freeEmbeddingTable(EmbeddingTable* e)
{
    for (int i = 0; i < e->vocabSize; i++)
        free(e->vectors[i]);

    free(e->vectors);
    e->vectors = NULL;
    e->vocabSize = 0;
}
#pragma once

typedef struct
{
    int vocabSize;
    int embeddingDim;
    double** vectors; // vectors[tokenId] is a length-embeddingDim vector
} EmbeddingTable;

void initEmbeddingTable(EmbeddingTable* e, int vocabSize, int embeddingDim);
const double* getEmbedding(EmbeddingTable* e, int tokenId);
void updateEmbedding(EmbeddingTable* e, int tokenId, const double* gradient, double lr);
void freeEmbeddingTable(EmbeddingTable* e);
// Module includes
#include "engine.h"
#include "BPE.h"
#include "network.h"
#include "embedding.h"
#include "cache.h"

// Core includes
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

// ---- Model shape -----------------------------------------------------
// Look up an embedding for each of the last CONTEXT_LEN tokens, concatenate
// them, and run that through a small MLP: sigmoid hidden layer(s), and a
// real softmax + cross-entropy output layer over the vocabulary (see
// layer.c / network.c). Nothing here is a transformer -- no attention, no
// positional structure beyond the fixed window -- but it's a real, trained
// next-token predictor built entirely on top of the existing Neuron/Layer/
// Network code.

// Fallback used only when CORPUS_FILE_PATH doesn't exist -- keeps the app
// runnable out of the box, but corpus.txt is what you actually want to
// grow for better output. Drop a much larger text file at that path and
// it'll be picked up (and retrained, since the cache is keyed on the
// corpus contents) on the next launch.
static const char* SEED_CORPUS =
    "hello how can i help you today "
    "i am still learning so my answers are limited right now "
    "thank you for trying me out ";

// Reads CORPUS_FILE_PATH into a malloc'd, null-terminated buffer. Falls
// back to a copy of SEED_CORPUS if the file doesn't exist or is empty.
static char* loadCorpus(void)
{
    FILE* f = fopen(CORPUS_FILE_PATH, "rb");
    if (!f)
    {
        char* copy = (char*)malloc(strlen(SEED_CORPUS) + 1);
        strcpy(copy, SEED_CORPUS);
        return copy;
    }

    fseek(f, 0, SEEK_END);
    long size = ftell(f);
    fseek(f, 0, SEEK_SET);

    if (size <= 0)
    {
        fclose(f);
        char* copy = (char*)malloc(strlen(SEED_CORPUS) + 1);
        strcpy(copy, SEED_CORPUS);
        return copy;
    }

    char* buf = (char*)malloc((size_t)size + 1);
    size_t n = fread(buf, 1, (size_t)size, f);
    buf[n] = '\0';
    fclose(f);

    return buf;
}

static Tokenizer tokenizer;
static EmbeddingTable embeddings;
static Network network;
static int engineReady = 0;

// Fills `input` with the concatenated embeddings for the CONTEXT_LEN token
// ids in `context`.
static void buildInputFromContext(const int* context, double* input)
{
    for (int c = 0; c < CONTEXT_LEN; c++)
    {
        const double* vec = getEmbedding(&embeddings, context[c]);
        memcpy(input + c * EMBEDDING_DIM, vec, sizeof(double) * EMBEDDING_DIM);
    }
}

// Trains on every (context -> next token) window in the tokenized corpus,
// for the given number of passes over the data.
static void trainOnCorpus(const char* corpus, int epochs)
{
    int corpusLen = (int)strlen(corpus);
    int* tokens = (int*)malloc(sizeof(int) * (corpusLen + 1));
    int tokenCount = tokenizeEncode(&tokenizer, corpus, tokens);

    if (tokenCount <= CONTEXT_LEN)
    {
        free(tokens);
        return; // not enough data to form even one training window
    }

    int vocabSize = tokenizer.vocabSize;
    double* target = (double*)malloc(sizeof(double) * vocabSize);
    double input[CONTEXT_LEN * EMBEDDING_DIM];
    double inputGrad[CONTEXT_LEN * EMBEDDING_DIM];

    for (int epoch = 0; epoch < epochs; epoch++)
    {
        for (int pos = CONTEXT_LEN; pos < tokenCount; pos++)
        {
            buildInputFromContext(&tokens[pos - CONTEXT_LEN], input);

            // One-hot target: the token that actually came next
            for (int v = 0; v < vocabSize; v++) target[v] = 0.0;
            int actualNext = tokens[pos];
            if (actualNext >= 0 && actualNext < vocabSize) target[actualNext] = 1.0;

            trainNetwork(&network, input, target);

            // Backprop one more step into the embeddings, using the same
            // "sum of next layer's deltas times its weights" rule the
            // network already uses to pass error between its own layers.
            // NOTE: trainNetwork has already updated layer 0's weights by
            // this point, so this uses post-update weights rather than the
            // ones actually used in the backward pass -- a one-step-stale
            // approximation. Fine at this toy scale; a from-scratch
            // implementation that needs to be exact would compute this
            // gradient before the weight update instead.
            Layer* firstLayer = &network.layers[0];
            for (int i = 0; i < CONTEXT_LEN * EMBEDDING_DIM; i++)
            {
                double sum = 0.0;
                for (int k = 0; k < firstLayer->numNeurons; k++)
                    sum += firstLayer->deltas[k] * firstLayer->neurons[k].weights[i];
                inputGrad[i] = sum;
            }

            for (int c = 0; c < CONTEXT_LEN; c++)
            {
                int ctxToken = tokens[pos - CONTEXT_LEN + c];
                updateEmbedding(&embeddings, ctxToken, inputGrad + c * EMBEDDING_DIM, LEARNING_RATE);
            }
        }
    }

    free(target);
    free(tokens);
}

void initEngine()
{
    srand((unsigned int)time(NULL));

    char* corpus = loadCorpus();

    if (loadModelCache(MODEL_CACHE_PATH, corpus, &tokenizer, &embeddings, &network))
    {
        // Cache hit: the corpus on disk matches what these were trained
        // on, so the tokenizer/embeddings/network we just loaded are
        // already trained -- nothing left to do.
        engineReady = 1;
        free(corpus);
        return;
    }

    trainBPE(&tokenizer, corpus, 1000);
    initEmbeddingTable(&embeddings, tokenizer.vocabSize, EMBEDDING_DIM);

    int sizes[] = { CONTEXT_LEN * EMBEDDING_DIM, HIDDEN_SIZE, tokenizer.vocabSize };
    initNetwork(&network, sizes, 2, LEARNING_RATE);

    engineReady = 1;

    // Blocking here is fine at this corpus size (a few thousand training
    // steps finishes well under a second) -- and now only happens on the
    // first run, or whenever corpus.txt actually changes, since the result
    // gets cached below. A much larger corpus would still make this worth
    // moving to a background thread for that first run.
    trainOnCorpus(corpus, TRAIN_EPOCHS);

    saveModelCache(MODEL_CACHE_PATH, corpus, &tokenizer, &embeddings, &network);

    free(corpus);
}

// Picks a token id from the network's output distribution. The output
// layer is a real softmax now (see layer.c / network.c), so `outputs`
// already sums to ~1.0 -- this still normalizes defensively against
// floating-point drift, but it's no longer doing the heavy lifting it
// used to when each output was an independent sigmoid.
static int sampleFromOutputs(const double* outputs, int vocabSize)
{
    double sum = 0.0;
    for (int i = 0; i < vocabSize; i++) sum += outputs[i];

    if (sum <= 0.0) return rand() % vocabSize; // nothing came out positive; fall back to uniform

    double r = ((double)rand() / (double)RAND_MAX) * sum;
    double acc = 0.0;
    for (int i = 0; i < vocabSize; i++)
    {
        acc += outputs[i];
        if (r <= acc) return i;
    }

    return vocabSize - 1;
}

// Generates up to maxTokens new token ids, starting from a context seeded
// with the tail of the prompt (padded with spaces if the prompt is
// shorter than CONTEXT_LEN), sliding the window forward one token at a
// time.
static int generateTokens(const int* promptTokens, int promptCount, int* outTokens, int maxTokens)
{
    int vocabSize = tokenizer.vocabSize;

    int context[CONTEXT_LEN];
    for (int i = 0; i < CONTEXT_LEN; i++)
    {
        int srcIdx = promptCount - CONTEXT_LEN + i;
        context[i] = (srcIdx >= 0) ? promptTokens[srcIdx] : PAD_TOKEN_ID;
    }

    double input[CONTEXT_LEN * EMBEDDING_DIM];
    int generatedCount = 0;

    for (int step = 0; step < maxTokens; step++)
    {
        buildInputFromContext(context, input);

        double* outputs = feedForwardNetwork(&network, input);
        int nextToken = sampleFromOutputs(outputs, vocabSize);

        outTokens[generatedCount++] = nextToken;

        for (int c = 0; c < CONTEXT_LEN - 1; c++)
            context[c] = context[c + 1];
        context[CONTEXT_LEN - 1] = nextToken;
    }

    return generatedCount;
}

void generateResponse(const char* userText, char* outputBuffer, int maxLen)
{
    if (!engineReady) initEngine();

    int len = (int)strlen(userText);
    int* promptTokens = (int*)malloc(sizeof(int) * (len + 1));
    int promptCount = tokenizeEncode(&tokenizer, userText, promptTokens);

    int generated[MAX_GENERATED_TOKENS];
    int generatedCount = generateTokens(promptTokens, promptCount, generated, MAX_GENERATED_TOKENS);

    tokenizeDecode(&tokenizer, generated, generatedCount, outputBuffer, maxLen);

    free(promptTokens);
}

int countTokens(const char* text)
{
    if (!engineReady) initEngine();

    int len = (int)strlen(text);
    if (len == 0) return 0;

    int* tokens = (int*)malloc(sizeof(int) * (len + 1));
    if (!tokens) return -1;

    int count = tokenizeEncode(&tokenizer, text, tokens);
    free(tokens);
    return count;
}

void freeEngine()
{
    if (engineReady)
    {
        freeTokenizer(&tokenizer);
        freeNetwork(&network);
        freeEmbeddingTable(&embeddings);
        engineReady = 0;
    }
}
#pragma once

#define CORPUS_FILE_PATH  "corpus.txt"
#define MODEL_CACHE_PATH  "model_cache.bin"
#define CONTEXT_LEN    16
#define EMBEDDING_DIM  128
#define HIDDEN_SIZE    512
#define LEARNING_RATE  0.5
#define TRAIN_EPOCHS   50
#define MAX_GENERATED_TOKENS 60
#define PAD_TOKEN_ID   32 // ASCII space, used to pad a context shorter than CONTEXT_LEN

void initEngine(void);
void generateResponse(const char* userText, char* outputBuffer, int maxLen);
int countTokens(const char* text);
void freeEngine(void);
// Module includes
#include "layer.h"

// Core includes
#include <stdlib.h>
#include <math.h>

void initLayer(Layer* layer, int numNeurons, int numInputs, double lr) 
{
    layer->numNeurons = numNeurons;
    layer->numInputs = numInputs;
    layer->isSoftmax = false; // hidden layers stay sigmoid; initNetwork flips the output layer

    layer->neurons = (Neuron*)malloc(sizeof(Neuron) * numNeurons);
    layer->outputs = (double*)malloc(sizeof(double) * numNeurons);
    layer->deltas = (double*)malloc(sizeof(double) * numNeurons);

    for (int i = 0; i < numNeurons; i++) 
        initNeuron(numInputs, lr, &layer->neurons[i]);
}

double* feedForwardLayer(Layer* layer, const double* inputs) 
{
    if (layer->isSoftmax)
    {
        // Raw logits first (no activation), stored directly into outputs
        // as scratch space -- normalized into real probabilities below.
        double maxRaw = -1.0e300;
        for (int j = 0; j < layer->numNeurons; j++)
        {
            Neuron* n = &layer->neurons[j];
            double sum = n->bias;
            for (int i = 0; i < layer->numInputs; i++)
                sum += inputs[i] * n->weights[i];

            layer->outputs[j] = sum;
            if (sum > maxRaw) maxRaw = sum;
        }

        // Subtracting the max before exponentiating keeps this numerically
        // stable without changing the result (softmax is shift-invariant).
        double expSum = 0.0;
        for (int j = 0; j < layer->numNeurons; j++)
        {
            double e = exp(layer->outputs[j] - maxRaw);
            layer->outputs[j] = e;
            expSum += e;
        }

        for (int j = 0; j < layer->numNeurons; j++)
            layer->outputs[j] /= expSum;

        return layer->outputs;
    }

    for (int i = 0; i < layer->numNeurons; i++)
        layer->outputs[i] = feedForward(inputs, layer->numInputs, &layer->neurons[i]);

    return layer->outputs;
}

void freeLayer(Layer* layer) 
{
    for (int i = 0; i < layer->numNeurons; i++)
        freeNeuron(&layer->neurons[i]);

    free(layer->neurons);
    free(layer->outputs);
    free(layer->deltas);
}
#pragma once

// Module include
#include "neuron.h"

// Core includes
#include <stdbool.h>

typedef struct 
{
    Neuron *neurons;
    int numNeurons;
    int numInputs;
    double* outputs;
    double* deltas;
    bool isSoftmax; // true only for the network's final (output) layer
}Layer;

void initLayer(Layer* layer, int numNeurons, int numInputs, double lr);
double* feedForwardLayer(Layer* layer, const double* inputs);
void freeLayer(Layer* layer);
// Module includes
#include "network.h"

// Core includes
#include <stdlib.h>

void initNetwork(Network* net, const int* layerSizes, int numLayers, double lr) 
{
    net->numLayers = numLayers;
    net->layers = (Layer*)malloc(sizeof(Layer) * numLayers);

    for (int i = 0; i < numLayers; i++) 
    {
        int inputsForThisLayer = layerSizes[i];
        int neuronsInThisLayer = layerSizes[i + 1];
        initLayer(&net->layers[i], neuronsInThisLayer, inputsForThisLayer, lr);
    }

    // The output layer predicts a probability distribution over the
    // vocabulary; hidden layers keep the sigmoid activation they've
    // always used.
    net->layers[numLayers - 1].isSoftmax = true;
}

double* feedForwardNetwork(Network* net, const double* inputs) 
{
    const double* current = inputs;

    for (int i = 0; i < net->numLayers; i++) 
        current = feedForwardLayer(&net->layers[i], current);

    return net->layers[net->numLayers - 1].outputs;
}

void trainNetwork(Network* net, const double* inputs, const double* targets) 
{
    feedForwardNetwork(net, inputs);

    Layer* outputLayer = &net->layers[net->numLayers - 1];

    if (outputLayer->isSoftmax)
    {
        // Softmax + cross-entropy: the softmax Jacobian and the
        // cross-entropy derivative cancel out algebraically, so the
        // gradient w.r.t. each output logit is simply (output - target).
        // To match this file's sign convention -- delta is defined as the
        // negative of that gradient, since weights are updated by adding
        // lr * delta * input -- that's (target - output), with no extra
        // derivative term needed (unlike the sigmoid+MSE case below).
        for (int j = 0; j < outputLayer->numNeurons; j++)
            outputLayer->deltas[j] = targets[j] - outputLayer->outputs[j];
    }
    else
    {
        for (int j = 0; j < outputLayer->numNeurons; j++) 
        {
            double output = outputLayer->outputs[j];
            double error = targets[j] - output;
            outputLayer->deltas[j] = error * output * (1.0 - output);
        }
    }

    for (int l = net->numLayers - 2; l >= 0; l--)
    {
        Layer* layer = &net->layers[l];
        Layer* nextLayer = &net->layers[l + 1];
 
        for (int j = 0; j < layer->numNeurons; j++)
        {
            double sum = 0.0;
            for (int k = 0; k < nextLayer->numNeurons; k++)
                sum += nextLayer->deltas[k] * nextLayer->neurons[k].weights[j];
 
            double output = layer->outputs[j];
            layer->deltas[j] = sum * output * (1.0 - output);
        }
    }

    for (int l = 0; l < net->numLayers; l++)
    {
        Layer* layer = &net->layers[l];
        const double* layerInputs = (l == 0) ? inputs : net->layers[l - 1].outputs;
 
        for (int j = 0; j < layer->numNeurons; j++)
        {
            Neuron* n = &layer->neurons[j];
 
            for (int i = 0; i < layer->numInputs; i++)
                n->weights[i] += n->learningRate * layer->deltas[j] * layerInputs[i];
 
            n->bias += n->learningRate * layer->deltas[j];
        }
    }

}

void freeNetwork(Network* net) 
{
    for (int i = 0; i < net->numLayers; i++) 
        freeLayer(&net->layers[i]);

    free(net->layers);
}
#pragma once

// Module includes
#include "layer.h"

typedef struct
{
    Layer* layers;
    int numLayers;
}Network;

void initNetwork(Network* net, const int* layerSizes, int numLayers, double lr);
double* feedForwardNetwork(Network* net, const double* inputs);
void trainNetwork(Network* net, const double* inputs, const double* targets);
void freeNetwork(Network* net);
// Library includes
#include "raylib.h"

// Core includes
#include <math.h>
#include <stdlib.h>

// Module includes
#include "neuron.h"

// Returns a random double in [-1.0, 1.0]
static double randomWeight(void)
{
    return GetRandomValue(-1000, 1000) / 1000.0;
}

void initNeuron(int numInputs, double lr, Neuron* neuron)
{
    neuron->learningRate = lr;
    neuron->numInputs = numInputs;
    neuron->bias = randomWeight();

    neuron->weights = (double*)malloc(sizeof(double) * numInputs);
    for (int i = 0; i < numInputs; ++i)
        neuron->weights[i] = randomWeight();
}

double feedForward(const double* inputs, int numInputs, Neuron* neuron)
{
    double sum = neuron->bias;

    for (int i = 0; i < numInputs; ++i)
        sum += inputs[i] * neuron->weights[i];

    return sigmoid(sum);
}

void trainNeuron(const double* inputs, int numInputs, double target, Neuron* neuron)
{
    double output = feedForward(inputs, numInputs, neuron);

    // Calculate error
    double error = target - output;

    // Derivative of sigmoid function
    double errorGradient = error * output * (1.0 - output);

    for (int i = 0; i < numInputs; ++i)
        neuron->weights[i] += neuron->learningRate * errorGradient * inputs[i];

    neuron->bias += neuron->learningRate * errorGradient;
}

double sigmoid(double x)
{
    return 1.0 / (1.0 + exp(-x));
}

void freeNeuron(Neuron* neuron)
{
    free(neuron->weights);
    neuron->weights = NULL;
}
#pragma once

typedef struct
{
    double* weights;
    int numInputs;
    double bias;
    double learningRate;
} Neuron;
 
void initNeuron(int numInputs, double lr, Neuron* neuron);
double feedForward(const double* inputs, int numInputs, Neuron* neuron);
void trainNeuron(const double* inputs, int numInputs, double target, Neuron* neuron);
double sigmoid(double x);
void freeNeuron(Neuron* neuron);
// Module includes
#include "chat_history.h"

// Core includes
#include <stddef.h>
#include <stdio.h>

static ChatMessage messages[MAX_CHAT_MESSAGES];
static int messageCount = 0;

void initChatHistory(void)
{
    messageCount = 0;
}

void addChatMessage(ChatRole role, const char* text)
{
    if (messageCount >= MAX_CHAT_MESSAGES) return; // TODO: evict oldest once history overflows

    messages[messageCount].role = role;

    int n = 0;
    while (text[n] != '\0' && n < MAX_MESSAGE_LENGTH - 1)
    {
        messages[messageCount].text[n] = text[n];
        n++;
    }
    messages[messageCount].text[n] = '\0';

    messageCount++;
}

int getChatMessageCount(void)
{
    return messageCount;
}

const ChatMessage* getChatMessage(int index)
{
    if (index < 0 || index >= messageCount) return NULL;
    return &messages[index];
}

void clearChatHistory(void)
{
    messageCount = 0;
}

bool saveChatHistoryToFile(const char* path)
{
    FILE* f = fopen(path, "w");
    if (!f) return false;

    for (int i = 0; i < messageCount; i++)
    {
        const char* roleLabel = (messages[i].role == ROLE_USER) ? "User" : "Assistant";
        fprintf(f, "%s:\n%s\n\n", roleLabel, messages[i].text);
    }

    fclose(f);
    return true;
}
#pragma once

#include <stdbool.h>

#define MAX_CHAT_MESSAGES 500
#define MAX_MESSAGE_LENGTH 4000

typedef enum
{
    ROLE_USER,
    ROLE_ASSISTANT
} ChatRole;

typedef struct
{
    ChatRole role;
    char text[MAX_MESSAGE_LENGTH];
} ChatMessage;

void initChatHistory(void);
void addChatMessage(ChatRole role, const char* text);
int getChatMessageCount(void);
const ChatMessage* getChatMessage(int index);
void clearChatHistory(void);
bool saveChatHistoryToFile(const char* path);
// ==== Python source: example functions ====

def factorial_258(x):
    result = 1
    for i in range(2, x + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of x by multiplying every integer from 2 up to x together, returning 1 for the base case where x is 0 or 1.

def binary_search_346(sorted_items, x):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == x:
            return mid
        elif sorted_items[mid] < x:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def transpose_143(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def swap_if_needed_190(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def clamp_187(count, lo, hi):
    if count < lo:
        return lo
    if count > hi:
        return hi
    return count
# Explanation: This function constrains count to lie between lo and hi, returning the nearest boundary value if count falls outside that range and count unchanged otherwise.

def transpose_487(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def transpose_898(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def sum_4(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def clamp_155(target, lo, hi):
    if target < lo:
        return lo
    if target > hi:
        return hi
    return target
# Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

def count_vowels_237(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def is_prime_688(x):
    if x < 2:
        return False
    for i in range(2, int(x ** 0.5) + 1):
        if x % i == 0:
            return False
    return True
# Explanation: This function checks whether x is a prime number by testing for divisors only up to the square root of x, since any larger factor would have a corresponding smaller factor already checked.

def count_vowels_458(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def factorial_847(data):
    result = 1
    for i in range(2, data + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of data by multiplying every integer from 2 up to data together, returning 1 for the base case where data is 0 or 1.

def linear_search_207(items, count):
    for i, val in enumerate(items):
        if val == count:
            return i
    return -1
# Explanation: This function searches items one element at a time for count, returning the index of the first match or -1 if count isn't found anywhere in items.

def factorial_43(num):
    result = 1
    for i in range(2, num + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of num by multiplying every integer from 2 up to num together, returning 1 for the base case where num is 0 or 1.

def count_words_852(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def count_vowels_327(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def clamp_655(x, lo, hi):
    if x < lo:
        return lo
    if x > hi:
        return hi
    return x
# Explanation: This function constrains x to lie between lo and hi, returning the nearest boundary value if x falls outside that range and x unchanged otherwise.

def transpose_585(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def max_of_470(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def max_of_614(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def factorial_348(target):
    result = 1
    for i in range(2, target + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

def sum_447(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def bubble_sort_485(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def count_vowels_100(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def is_prime_855(value):
    if value < 2:
        return False
    for i in range(2, int(value ** 0.5) + 1):
        if value % i == 0:
            return False
    return True
# Explanation: This function checks whether value is a prime number by testing for divisors only up to the square root of value, since any larger factor would have a corresponding smaller factor already checked.

def stack_peek_or_default_95(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def count_vowels_136(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def power_64(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def swap_if_needed_786(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def reverse_916(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def swap_if_needed_576(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def is_palindrome_738(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def transpose_505(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def power_475(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def binary_search_77(sorted_items, data):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == data:
            return mid
        elif sorted_items[mid] < data:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds data by repeatedly halving the search range: comparing data against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def factorial_19(target):
    result = 1
    for i in range(2, target + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

def count_words_698(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def power_404(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def gcd_814(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def gcd_69(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def binary_search_731(sorted_items, n):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == n:
            return mid
        elif sorted_items[mid] < n:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds n by repeatedly halving the search range: comparing n against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def average_796(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def count_words_276(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def gcd_502(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def factorial_73(count):
    result = 1
    for i in range(2, count + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

def clamp_971(n, lo, hi):
    if n < lo:
        return lo
    if n > hi:
        return hi
    return n
# Explanation: This function constrains n to lie between lo and hi, returning the nearest boundary value if n falls outside that range and n unchanged otherwise.

def is_palindrome_769(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def linear_search_131(items, n):
    for i, val in enumerate(items):
        if val == n:
            return i
    return -1
# Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

def is_palindrome_996(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def swap_if_needed_276(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def bubble_sort_427(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def average_528(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def reverse_520(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def bubble_sort_963(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def bubble_sort_674(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def is_prime_767(target):
    if target < 2:
        return False
    for i in range(2, int(target ** 0.5) + 1):
        if target % i == 0:
            return False
    return True
# Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

def is_prime_740(total):
    if total < 2:
        return False
    for i in range(2, int(total ** 0.5) + 1):
        if total % i == 0:
            return False
    return True
# Explanation: This function checks whether total is a prime number by testing for divisors only up to the square root of total, since any larger factor would have a corresponding smaller factor already checked.

def sum_637(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def fibonacci_847(data):
    a, b = 0, 1
    for _ in range(data):
        a, b = b, a + b
    return a
# Explanation: This function returns the data-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def sum_976(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def count_words_594(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def power_400(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def bubble_sort_550(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def binary_search_861(sorted_items, amount):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == amount:
            return mid
        elif sorted_items[mid] < amount:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def stack_peek_or_default_730(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def gcd_155(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def is_prime_487(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def bubble_sort_256(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def factorial_29(count):
    result = 1
    for i in range(2, count + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

def sum_979(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def transpose_205(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def swap_if_needed_826(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def transpose_287(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def count_vowels_920(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def count_vowels_307(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def swap_if_needed_679(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def factorial_73(x):
    result = 1
    for i in range(2, x + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of x by multiplying every integer from 2 up to x together, returning 1 for the base case where x is 0 or 1.

def count_vowels_188(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def max_of_224(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def swap_if_needed_478(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def swap_if_needed_295(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def fibonacci_986(value):
    a, b = 0, 1
    for _ in range(value):
        a, b = b, a + b
    return a
# Explanation: This function returns the value-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def transpose_507(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def bubble_sort_45(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def count_words_718(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def gcd_961(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def is_palindrome_747(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def is_prime_713(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def count_words_63(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def fibonacci_288(value):
    a, b = 0, 1
    for _ in range(value):
        a, b = b, a + b
    return a
# Explanation: This function returns the value-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def bubble_sort_840(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def count_vowels_386(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def swap_if_needed_109(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def is_palindrome_261(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def count_words_674(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def reverse_87(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def clamp_60(target, lo, hi):
    if target < lo:
        return lo
    if target > hi:
        return hi
    return target
# Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

def factorial_819(value):
    result = 1
    for i in range(2, value + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

def count_words_404(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def binary_search_692(sorted_items, amount):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == amount:
            return mid
        elif sorted_items[mid] < amount:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def count_vowels_83(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def transpose_68(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def sum_945(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def gcd_716(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def gcd_958(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def reverse_375(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def power_758(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def count_words_18(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def is_prime_867(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def count_vowels_67(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def is_prime_578(n):
    if n < 2:
        return False
    for i in range(2, int(n ** 0.5) + 1):
        if n % i == 0:
            return False
    return True
# Explanation: This function checks whether n is a prime number by testing for divisors only up to the square root of n, since any larger factor would have a corresponding smaller factor already checked.

def count_words_167(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def is_prime_181(num):
    if num < 2:
        return False
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            return False
    return True
# Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

def is_palindrome_124(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def max_of_544(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def is_prime_148(size):
    if size < 2:
        return False
    for i in range(2, int(size ** 0.5) + 1):
        if size % i == 0:
            return False
    return True
# Explanation: This function checks whether size is a prime number by testing for divisors only up to the square root of size, since any larger factor would have a corresponding smaller factor already checked.

def count_vowels_629(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def transpose_134(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def fibonacci_632(x):
    a, b = 0, 1
    for _ in range(x):
        a, b = b, a + b
    return a
# Explanation: This function returns the x-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def average_168(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def sum_491(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def fibonacci_334(target):
    a, b = 0, 1
    for _ in range(target):
        a, b = b, a + b
    return a
# Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def count_words_432(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def factorial_344(size):
    result = 1
    for i in range(2, size + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of size by multiplying every integer from 2 up to size together, returning 1 for the base case where size is 0 or 1.

def max_of_786(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def swap_if_needed_71(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def transpose_324(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def swap_if_needed_364(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def transpose_583(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def average_75(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def count_words_866(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def stack_peek_or_default_562(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def average_774(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def binary_search_846(sorted_items, x):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == x:
            return mid
        elif sorted_items[mid] < x:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def sum_884(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def binary_search_237(sorted_items, amount):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == amount:
            return mid
        elif sorted_items[mid] < amount:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def fibonacci_858(num):
    a, b = 0, 1
    for _ in range(num):
        a, b = b, a + b
    return a
# Explanation: This function returns the num-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def transpose_338(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def transpose_768(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def count_vowels_486(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def reverse_119(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def sum_464(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def count_vowels_147(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def swap_if_needed_334(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def sum_416(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def transpose_771(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def is_prime_63(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def binary_search_69(sorted_items, x):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == x:
            return mid
        elif sorted_items[mid] < x:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def is_palindrome_349(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def power_348(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def binary_search_297(sorted_items, amount):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == amount:
            return mid
        elif sorted_items[mid] < amount:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def sum_59(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def binary_search_826(sorted_items, x):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == x:
            return mid
        elif sorted_items[mid] < x:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def factorial_894(amount):
    result = 1
    for i in range(2, amount + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of amount by multiplying every integer from 2 up to amount together, returning 1 for the base case where amount is 0 or 1.

def count_words_804(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def stack_peek_or_default_880(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def count_words_259(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def reverse_570(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def is_prime_121(target):
    if target < 2:
        return False
    for i in range(2, int(target ** 0.5) + 1):
        if target % i == 0:
            return False
    return True
# Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

def is_prime_116(x):
    if x < 2:
        return False
    for i in range(2, int(x ** 0.5) + 1):
        if x % i == 0:
            return False
    return True
# Explanation: This function checks whether x is a prime number by testing for divisors only up to the square root of x, since any larger factor would have a corresponding smaller factor already checked.

def average_429(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def linear_search_626(items, value):
    for i, val in enumerate(items):
        if val == value:
            return i
    return -1
# Explanation: This function searches items one element at a time for value, returning the index of the first match or -1 if value isn't found anywhere in items.

def clamp_600(size, lo, hi):
    if size < lo:
        return lo
    if size > hi:
        return hi
    return size
# Explanation: This function constrains size to lie between lo and hi, returning the nearest boundary value if size falls outside that range and size unchanged otherwise.

def bubble_sort_105(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def stack_peek_or_default_332(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def reverse_721(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def transpose_639(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def transpose_293(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def count_words_933(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def average_949(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def power_783(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def bubble_sort_63(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def transpose_643(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def reverse_709(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def is_palindrome_477(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def bubble_sort_430(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def power_981(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def binary_search_787(sorted_items, count):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == count:
            return mid
        elif sorted_items[mid] < count:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds count by repeatedly halving the search range: comparing count against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def swap_if_needed_817(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def power_939(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def swap_if_needed_572(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def transpose_687(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def power_909(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def sum_905(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def count_vowels_24(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def average_148(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def count_words_640(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def max_of_5(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def bubble_sort_405(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def max_of_734(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def is_palindrome_759(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def gcd_196(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def power_153(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def is_prime_146(target):
    if target < 2:
        return False
    for i in range(2, int(target ** 0.5) + 1):
        if target % i == 0:
            return False
    return True
# Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

def transpose_547(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def reverse_349(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def is_prime_840(target):
    if target < 2:
        return False
    for i in range(2, int(target ** 0.5) + 1):
        if target % i == 0:
            return False
    return True
# Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

def stack_peek_or_default_435(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def gcd_105(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def is_palindrome_860(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def count_vowels_617(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def max_of_659(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def clamp_137(n, lo, hi):
    if n < lo:
        return lo
    if n > hi:
        return hi
    return n
# Explanation: This function constrains n to lie between lo and hi, returning the nearest boundary value if n falls outside that range and n unchanged otherwise.

def gcd_408(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def transpose_672(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def power_356(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def count_vowels_983(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def is_palindrome_92(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def reverse_905(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def reverse_386(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def factorial_270(total):
    result = 1
    for i in range(2, total + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

def binary_search_39(sorted_items, value):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == value:
            return mid
        elif sorted_items[mid] < value:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds value by repeatedly halving the search range: comparing value against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def fibonacci_855(count):
    a, b = 0, 1
    for _ in range(count):
        a, b = b, a + b
    return a
# Explanation: This function returns the count-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def transpose_974(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def bubble_sort_177(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def transpose_621(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def count_vowels_151(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def is_prime_247(value):
    if value < 2:
        return False
    for i in range(2, int(value ** 0.5) + 1):
        if value % i == 0:
            return False
    return True
# Explanation: This function checks whether value is a prime number by testing for divisors only up to the square root of value, since any larger factor would have a corresponding smaller factor already checked.

def bubble_sort_839(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def bubble_sort_605(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def count_words_83(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def factorial_163(n):
    result = 1
    for i in range(2, n + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of n by multiplying every integer from 2 up to n together, returning 1 for the base case where n is 0 or 1.

def count_words_266(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def stack_peek_or_default_285(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def max_of_525(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def linear_search_624(items, n):
    for i, val in enumerate(items):
        if val == n:
            return i
    return -1
# Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

def power_408(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def is_prime_615(num):
    if num < 2:
        return False
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            return False
    return True
# Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

def gcd_205(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def fibonacci_87(size):
    a, b = 0, 1
    for _ in range(size):
        a, b = b, a + b
    return a
# Explanation: This function returns the size-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def power_762(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def fibonacci_75(target):
    a, b = 0, 1
    for _ in range(target):
        a, b = b, a + b
    return a
# Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def binary_search_853(sorted_items, num):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == num:
            return mid
        elif sorted_items[mid] < num:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds num by repeatedly halving the search range: comparing num against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def gcd_783(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def reverse_408(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def bubble_sort_757(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def average_368(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def max_of_88(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def clamp_698(amount, lo, hi):
    if amount < lo:
        return lo
    if amount > hi:
        return hi
    return amount
# Explanation: This function constrains amount to lie between lo and hi, returning the nearest boundary value if amount falls outside that range and amount unchanged otherwise.

def bubble_sort_445(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def average_809(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def stack_peek_or_default_379(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def stack_peek_or_default_121(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def bubble_sort_427(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def clamp_946(amount, lo, hi):
    if amount < lo:
        return lo
    if amount > hi:
        return hi
    return amount
# Explanation: This function constrains amount to lie between lo and hi, returning the nearest boundary value if amount falls outside that range and amount unchanged otherwise.

def binary_search_686(sorted_items, count):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == count:
            return mid
        elif sorted_items[mid] < count:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds count by repeatedly halving the search range: comparing count against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def factorial_779(value):
    result = 1
    for i in range(2, value + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

def linear_search_365(items, x):
    for i, val in enumerate(items):
        if val == x:
            return i
    return -1
# Explanation: This function searches items one element at a time for x, returning the index of the first match or -1 if x isn't found anywhere in items.

def stack_peek_or_default_156(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def is_palindrome_89(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def binary_search_236(sorted_items, total):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == total:
            return mid
        elif sorted_items[mid] < total:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds total by repeatedly halving the search range: comparing total against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def average_847(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def is_prime_589(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def transpose_689(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def stack_peek_or_default_430(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def clamp_316(target, lo, hi):
    if target < lo:
        return lo
    if target > hi:
        return hi
    return target
# Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

def count_vowels_749(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def bubble_sort_45(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def gcd_36(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def linear_search_106(items, data):
    for i, val in enumerate(items):
        if val == data:
            return i
    return -1
# Explanation: This function searches items one element at a time for data, returning the index of the first match or -1 if data isn't found anywhere in items.

def count_vowels_609(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def reverse_465(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def gcd_710(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def clamp_327(num, lo, hi):
    if num < lo:
        return lo
    if num > hi:
        return hi
    return num
# Explanation: This function constrains num to lie between lo and hi, returning the nearest boundary value if num falls outside that range and num unchanged otherwise.

def fibonacci_468(target):
    a, b = 0, 1
    for _ in range(target):
        a, b = b, a + b
    return a
# Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def gcd_628(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def count_words_61(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def clamp_981(target, lo, hi):
    if target < lo:
        return lo
    if target > hi:
        return hi
    return target
# Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

def sum_961(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def transpose_727(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def count_words_154(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def bubble_sort_991(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def average_169(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def is_palindrome_971(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def stack_peek_or_default_195(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def linear_search_269(items, n):
    for i, val in enumerate(items):
        if val == n:
            return i
    return -1
# Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

def sum_279(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def bubble_sort_297(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def swap_if_needed_416(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def gcd_646(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def clamp_835(target, lo, hi):
    if target < lo:
        return lo
    if target > hi:
        return hi
    return target
# Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

def average_870(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def linear_search_436(items, value):
    for i, val in enumerate(items):
        if val == value:
            return i
    return -1
# Explanation: This function searches items one element at a time for value, returning the index of the first match or -1 if value isn't found anywhere in items.

def sum_875(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def fibonacci_643(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
# Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def gcd_482(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def gcd_88(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def count_words_714(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def transpose_376(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def linear_search_276(items, amount):
    for i, val in enumerate(items):
        if val == amount:
            return i
    return -1
# Explanation: This function searches items one element at a time for amount, returning the index of the first match or -1 if amount isn't found anywhere in items.

def transpose_316(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def average_885(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def count_words_520(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def average_752(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def bubble_sort_627(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def count_words_560(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def linear_search_716(items, x):
    for i, val in enumerate(items):
        if val == x:
            return i
    return -1
# Explanation: This function searches items one element at a time for x, returning the index of the first match or -1 if x isn't found anywhere in items.

def stack_peek_or_default_59(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def is_prime_743(num):
    if num < 2:
        return False
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            return False
    return True
# Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

def swap_if_needed_324(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def factorial_777(target):
    result = 1
    for i in range(2, target + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

def max_of_47(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def stack_peek_or_default_698(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def reverse_919(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def swap_if_needed_452(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def reverse_971(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def linear_search_529(items, n):
    for i, val in enumerate(items):
        if val == n:
            return i
    return -1
# Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

def max_of_742(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def power_802(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def stack_peek_or_default_915(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def power_215(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def transpose_523(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def power_79(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def factorial_307(count):
    result = 1
    for i in range(2, count + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

def count_words_920(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def reverse_972(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def is_prime_968(count):
    if count < 2:
        return False
    for i in range(2, int(count ** 0.5) + 1):
        if count % i == 0:
            return False
    return True
# Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

def count_vowels_978(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def count_words_496(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

def binary_search_255(sorted_items, target):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == target:
            return mid
        elif sorted_items[mid] < target:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def gcd_13(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def fibonacci_928(num):
    a, b = 0, 1
    for _ in range(num):
        a, b = b, a + b
    return a
# Explanation: This function returns the num-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def power_123(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def transpose_569(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            result[j][i] = matrix[i][j]
    return result
# Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

def sum_86(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def stack_peek_or_default_555(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def max_of_745(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def average_673(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def power_994(base, exponent):
    result = 1
    for _ in range(exponent):
        result *= base
    return result
# Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

def stack_peek_or_default_937(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def is_palindrome_156(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def reverse_494(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def is_palindrome_2(text):
    cleaned = text.lower().replace(" ", "")
    return cleaned == cleaned[::-1]
# Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

def swap_if_needed_167(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def linear_search_460(items, size):
    for i, val in enumerate(items):
        if val == size:
            return i
    return -1
# Explanation: This function searches items one element at a time for size, returning the index of the first match or -1 if size isn't found anywhere in items.

def swap_if_needed_225(a, b):
    if a > b:
        a, b = b, a
    return a, b
# Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

def gcd_87(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def linear_search_278(items, total):
    for i, val in enumerate(items):
        if val == total:
            return i
    return -1
# Explanation: This function searches items one element at a time for total, returning the index of the first match or -1 if total isn't found anywhere in items.

def clamp_334(count, lo, hi):
    if count < lo:
        return lo
    if count > hi:
        return hi
    return count
# Explanation: This function constrains count to lie between lo and hi, returning the nearest boundary value if count falls outside that range and count unchanged otherwise.

def binary_search_710(sorted_items, data):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == data:
            return mid
        elif sorted_items[mid] < data:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds data by repeatedly halving the search range: comparing data against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def max_of_88(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def average_201(values):
    if not values:
        return 0
    return sum(values) / len(values)
# Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

def binary_search_490(sorted_items, num):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == num:
            return mid
        elif sorted_items[mid] < num:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds num by repeatedly halving the search range: comparing num against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def stack_peek_or_default_205(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def factorial_491(total):
    result = 1
    for i in range(2, total + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

def max_of_514(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def count_vowels_160(text):
    vowels = "aeiouAEIOU"
    return sum(1 for c in text if c in vowels)
# Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

def sum_44(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def factorial_394(total):
    result = 1
    for i in range(2, total + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

def reverse_993(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def fibonacci_105(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
# Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def stack_peek_or_default_286(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def clamp_79(total, lo, hi):
    if total < lo:
        return lo
    if total > hi:
        return hi
    return total
# Explanation: This function constrains total to lie between lo and hi, returning the nearest boundary value if total falls outside that range and total unchanged otherwise.

def max_of_260(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def factorial_257(n):
    result = 1
    for i in range(2, n + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of n by multiplying every integer from 2 up to n together, returning 1 for the base case where n is 0 or 1.

def bubble_sort_22(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

def max_of_726(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def gcd_675(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def gcd_747(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def factorial_650(value):
    result = 1
    for i in range(2, value + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

def factorial_347(target):
    result = 1
    for i in range(2, target + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

def binary_search_509(sorted_items, target):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == target:
            return mid
        elif sorted_items[mid] < target:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def stack_peek_or_default_587(stack_items, default_value):
    if len(stack_items) == 0:
        return default_value
    return stack_items[-1]
# Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

def binary_search_974(sorted_items, target):
    lo, hi = 0, len(sorted_items) - 1
    while lo <= hi:
        mid = (lo + hi) // 2
        if sorted_items[mid] == target:
            return mid
        elif sorted_items[mid] < target:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
# Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

def factorial_94(count):
    result = 1
    for i in range(2, count + 1):
        result *= i
    return result
# Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

def clamp_43(size, lo, hi):
    if size < lo:
        return lo
    if size > hi:
        return hi
    return size
# Explanation: This function constrains size to lie between lo and hi, returning the nearest boundary value if size falls outside that range and size unchanged otherwise.

def gcd_166(a, b):
    while b:
        a, b = b, a % b
    return a
# Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

def max_of_637(values):
    best = values[0]
    for x in values[1:]:
        if x > best:
            best = x
    return best
# Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

def fibonacci_227(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
# Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

def sum_774(values):
    total = 0
    for x in values:
        total += x
    return total
# Explanation: This function adds up every element in values and returns the total.

def reverse_402(text):
    return text[::-1]
# Explanation: This function returns text with its characters in reverse order.

def count_words_478(text):
    return len(text.split())
# Explanation: This function counts the number of whitespace-separated words in text.

// ==== C++ source: example functions ====

int linear_search_269(const std::vector<int>& items, int n) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == n) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

int clamp_43(int size, int lo, int hi) {
    if (size < lo) return lo;
    if (size > hi) return hi;
    return size;
}
// Explanation: This function constrains size to lie between lo and hi, returning the nearest boundary value if size falls outside that range and size unchanged otherwise.

int count_vowels_978(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long power_475(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int count_vowels_629(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

std::vector<std::vector<int>> transpose_143(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

void swap_if_needed_167(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

std::vector<std::vector<int>> transpose_134(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int count_words_594(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

long long power_939(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int stack_peek_or_default_285(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

bool is_palindrome_124(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

long long factorial_347(int target) {
    long long result = 1;
    for (int i = 2; i <= target; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

long long factorial_29(int count) {
    long long result = 1;
    for (int i = 2; i <= count; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

int stack_peek_or_default_880(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

std::vector<std::vector<int>> transpose_547(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

std::string reverse_919(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int clamp_971(int n, int lo, int hi) {
    if (n < lo) return lo;
    if (n > hi) return hi;
    return n;
}
// Explanation: This function constrains n to lie between lo and hi, returning the nearest boundary value if n falls outside that range and n unchanged otherwise.

double average_528(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

bool is_prime_578(int n) {
    if (n < 2) return false;
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether n is a prime number by testing for divisors only up to the square root of n, since any larger factor would have a corresponding smaller factor already checked.

int stack_peek_or_default_937(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int count_vowels_486(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int binary_search_297(const std::vector<int>& sorted_items, int amount) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == amount) return mid;
        else if (sorted_items[mid] < amount) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int gcd_482(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

long long factorial_43(int num) {
    long long result = 1;
    for (int i = 2; i <= num; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of num by multiplying every integer from 2 up to num together, returning 1 for the base case where num is 0 or 1.

int binary_search_69(const std::vector<int>& sorted_items, int x) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == x) return mid;
        else if (sorted_items[mid] < x) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int max_of_47(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

bool is_prime_713(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

int linear_search_624(const std::vector<int>& items, int n) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == n) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

std::vector<std::vector<int>> transpose_621(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int count_words_63(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

double average_949(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int clamp_981(int target, int lo, int hi) {
    if (target < lo) return lo;
    if (target > hi) return hi;
    return target;
}
// Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

bool is_prime_855(int value) {
    if (value < 2) return false;
    for (int i = 2; i * i <= value; i++) {
        if (value % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether value is a prime number by testing for divisors only up to the square root of value, since any larger factor would have a corresponding smaller factor already checked.

void swap_if_needed_364(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

int stack_peek_or_default_379(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

void bubble_sort_840(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

bool is_palindrome_996(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

void bubble_sort_627(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int count_vowels_609(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

void swap_if_needed_576(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long power_153(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

long long fibonacci_632(int x) {
    long long a = 0, b = 1;
    for (int i = 0; i < x; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the x-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int max_of_614(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

bool is_prime_121(int target) {
    if (target < 2) return false;
    for (int i = 2; i * i <= target; i++) {
        if (target % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

int stack_peek_or_default_286(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

std::string reverse_916(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

void bubble_sort_405(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int count_vowels_386(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

bool is_palindrome_92(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

long long fibonacci_334(int target) {
    long long a = 0, b = 1;
    for (int i = 0; i < target; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

long long fibonacci_643(int n) {
    long long a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

long long factorial_257(int n) {
    long long result = 1;
    for (int i = 2; i <= n; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of n by multiplying every integer from 2 up to n together, returning 1 for the base case where n is 0 or 1.

int stack_peek_or_default_435(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

std::vector<std::vector<int>> transpose_672(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_69(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

double average_870(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int max_of_745(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

long long factorial_270(int total) {
    long long result = 1;
    for (int i = 2; i <= total; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

bool is_prime_181(int num) {
    if (num < 2) return false;
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

std::vector<std::vector<int>> transpose_287(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_105(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

void bubble_sort_963(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int linear_search_131(const std::vector<int>& items, int n) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == n) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

int count_words_866(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int stack_peek_or_default_59(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int max_of_470(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

std::vector<std::vector<int>> transpose_68(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long power_994(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

bool is_palindrome_738(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

double average_169(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

bool is_prime_148(int size) {
    if (size < 2) return false;
    for (int i = 2; i * i <= size; i++) {
        if (size % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether size is a prime number by testing for divisors only up to the square root of size, since any larger factor would have a corresponding smaller factor already checked.

int gcd_961(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int count_vowels_307(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int max_of_88(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

long long factorial_258(int x) {
    long long result = 1;
    for (int i = 2; i <= x; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of x by multiplying every integer from 2 up to x together, returning 1 for the base case where x is 0 or 1.

double average_809(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int max_of_726(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

std::vector<std::vector<int>> transpose_727(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

bool is_palindrome_89(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int linear_search_276(const std::vector<int>& items, int amount) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == amount) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for amount, returning the index of the first match or -1 if amount isn't found anywhere in items.

bool is_palindrome_759(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

std::vector<std::vector<int>> transpose_205(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int binary_search_692(const std::vector<int>& sorted_items, int amount) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == amount) return mid;
        else if (sorted_items[mid] < amount) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

long long fibonacci_928(int num) {
    long long a = 0, b = 1;
    for (int i = 0; i < num; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the num-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

std::vector<std::vector<int>> transpose_768(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_155(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int binary_search_710(const std::vector<int>& sorted_items, int data) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == data) return mid;
        else if (sorted_items[mid] < data) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds data by repeatedly halving the search range: comparing data against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

void swap_if_needed_276(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

int clamp_155(int target, int lo, int hi) {
    if (target < lo) return lo;
    if (target > hi) return hi;
    return target;
}
// Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

long long power_356(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int count_vowels_67(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int gcd_13(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

void bubble_sort_757(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

bool is_prime_116(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++) {
        if (x % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether x is a prime number by testing for divisors only up to the square root of x, since any larger factor would have a corresponding smaller factor already checked.

int count_vowels_151(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

void swap_if_needed_826(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long power_981(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

long long fibonacci_227(int n) {
    long long a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int count_words_698(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int count_words_520(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

bool is_prime_63(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

long long sum_59(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

std::string reverse_349(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

void swap_if_needed_295(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long power_404(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

void bubble_sort_22(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

void swap_if_needed_572(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

void bubble_sort_63(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

std::string reverse_721(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int clamp_60(int target, int lo, int hi) {
    if (target < lo) return lo;
    if (target > hi) return hi;
    return target;
}
// Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

int count_words_714(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

void bubble_sort_427(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

double average_75(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

long long factorial_894(int amount) {
    long long result = 1;
    for (int i = 2; i <= amount; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of amount by multiplying every integer from 2 up to amount together, returning 1 for the base case where amount is 0 or 1.

std::vector<std::vector<int>> transpose_687(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long power_762(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

void bubble_sort_430(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int gcd_710(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int count_vowels_237(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long sum_945(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

long long factorial_819(int value) {
    long long result = 1;
    for (int i = 2; i <= value; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

double average_168(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

bool is_palindrome_2(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int max_of_224(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

int count_vowels_136(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int binary_search_826(const std::vector<int>& sorted_items, int x) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == x) return mid;
        else if (sorted_items[mid] < x) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int gcd_675(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int binary_search_686(const std::vector<int>& sorted_items, int count) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == count) return mid;
        else if (sorted_items[mid] < count) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds count by repeatedly halving the search range: comparing count against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

double average_752(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

double average_885(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

std::string reverse_972(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int gcd_747(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

void bubble_sort_550(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

bool is_palindrome_349(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int clamp_187(int count, int lo, int hi) {
    if (count < lo) return lo;
    if (count > hi) return hi;
    return count;
}
// Explanation: This function constrains count to lie between lo and hi, returning the nearest boundary value if count falls outside that range and count unchanged otherwise.

int count_words_804(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int count_words_404(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

long long factorial_19(int target) {
    long long result = 1;
    for (int i = 2; i <= target; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

int clamp_946(int amount, int lo, int hi) {
    if (amount < lo) return lo;
    if (amount > hi) return hi;
    return amount;
}
// Explanation: This function constrains amount to lie between lo and hi, returning the nearest boundary value if amount falls outside that range and amount unchanged otherwise.

bool is_prime_767(int target) {
    if (target < 2) return false;
    for (int i = 2; i * i <= target; i++) {
        if (target % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

double average_429(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int count_words_560(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

void bubble_sort_256(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int clamp_334(int count, int lo, int hi) {
    if (count < lo) return lo;
    if (count > hi) return hi;
    return count;
}
// Explanation: This function constrains count to lie between lo and hi, returning the nearest boundary value if count falls outside that range and count unchanged otherwise.

bool is_palindrome_769(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

long long factorial_163(int n) {
    long long result = 1;
    for (int i = 2; i <= n; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of n by multiplying every integer from 2 up to n together, returning 1 for the base case where n is 0 or 1.

std::vector<std::vector<int>> transpose_487(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

void bubble_sort_45(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int clamp_655(int x, int lo, int hi) {
    if (x < lo) return lo;
    if (x > hi) return hi;
    return x;
}
// Explanation: This function constrains x to lie between lo and hi, returning the nearest boundary value if x falls outside that range and x unchanged otherwise.

long long fibonacci_288(int value) {
    long long a = 0, b = 1;
    for (int i = 0; i < value; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the value-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int max_of_514(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

int count_words_718(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

bool is_prime_615(int num) {
    if (num < 2) return false;
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

int count_words_640(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

std::vector<std::vector<int>> transpose_338(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int stack_peek_or_default_730(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

long long sum_979(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

std::vector<std::vector<int>> transpose_376(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_88(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int max_of_544(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

double average_148(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

double average_673(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int linear_search_436(const std::vector<int>& items, int value) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == value) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for value, returning the index of the first match or -1 if value isn't found anywhere in items.

std::vector<std::vector<int>> transpose_585(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long sum_44(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int gcd_205(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int count_vowels_147(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long power_400(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int count_words_83(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int count_words_920(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int gcd_958(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

bool is_prime_487(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

bool is_palindrome_971(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

std::vector<std::vector<int>> transpose_293(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long fibonacci_87(int size) {
    long long a = 0, b = 1;
    for (int i = 0; i < size; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the size-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int stack_peek_or_default_555(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

double average_774(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

int gcd_628(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

bool is_prime_867(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

std::string reverse_570(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

long long sum_905(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int max_of_659(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

int gcd_166(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

void bubble_sort_445(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int binary_search_731(const std::vector<int>& sorted_items, int n) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == n) return mid;
        else if (sorted_items[mid] < n) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds n by repeatedly halving the search range: comparing n against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

long long factorial_777(int target) {
    long long result = 1;
    for (int i = 2; i <= target; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

void bubble_sort_177(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

std::vector<std::vector<int>> transpose_974(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

double average_847(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

void bubble_sort_605(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

long long power_758(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int gcd_783(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::string reverse_465(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int gcd_814(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::vector<std::vector<int>> transpose_316(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

std::vector<std::vector<int>> transpose_569(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_87(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::string reverse_905(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

bool is_prime_688(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++) {
        if (x % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether x is a prime number by testing for divisors only up to the square root of x, since any larger factor would have a corresponding smaller factor already checked.

int count_words_674(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

long long sum_279(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int clamp_698(int amount, int lo, int hi) {
    if (amount < lo) return lo;
    if (amount > hi) return hi;
    return amount;
}
// Explanation: This function constrains amount to lie between lo and hi, returning the nearest boundary value if amount falls outside that range and amount unchanged otherwise.

int gcd_716(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

void bubble_sort_839(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int binary_search_77(const std::vector<int>& sorted_items, int data) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == data) return mid;
        else if (sorted_items[mid] < data) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds data by repeatedly halving the search range: comparing data against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int linear_search_716(const std::vector<int>& items, int x) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == x) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for x, returning the index of the first match or -1 if x isn't found anywhere in items.

bool is_prime_840(int target) {
    if (target < 2) return false;
    for (int i = 2; i * i <= target; i++) {
        if (target % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

int count_words_18(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

long long fibonacci_986(int value) {
    long long a = 0, b = 1;
    for (int i = 0; i < value; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the value-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

long long power_802(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

long long sum_491(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

std::string reverse_971(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int count_words_61(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

std::vector<std::vector<int>> transpose_324(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int gcd_36(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int count_words_266(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

void bubble_sort_674(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

std::vector<std::vector<int>> transpose_689(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int count_vowels_188(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

void bubble_sort_45(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int linear_search_529(const std::vector<int>& items, int n) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == n) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for n, returning the index of the first match or -1 if n isn't found anywhere in items.

long long factorial_650(int value) {
    long long result = 1;
    for (int i = 2; i <= value; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

int binary_search_787(const std::vector<int>& sorted_items, int count) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == count) return mid;
        else if (sorted_items[mid] < count) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds count by repeatedly halving the search range: comparing count against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

long long sum_774(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

void swap_if_needed_225(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

std::vector<std::vector<int>> transpose_523(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int stack_peek_or_default_587(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

void swap_if_needed_452(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

std::string reverse_520(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

void swap_if_needed_679(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long power_783(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

long long sum_86(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int max_of_637(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

bool is_prime_146(int target) {
    if (target < 2) return false;
    for (int i = 2; i * i <= target; i++) {
        if (target % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether target is a prime number by testing for divisors only up to the square root of target, since any larger factor would have a corresponding smaller factor already checked.

int stack_peek_or_default_156(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int stack_peek_or_default_430(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int clamp_600(int size, int lo, int hi) {
    if (size < lo) return lo;
    if (size > hi) return hi;
    return size;
}
// Explanation: This function constrains size to lie between lo and hi, returning the nearest boundary value if size falls outside that range and size unchanged otherwise.

long long factorial_344(int size) {
    long long result = 1;
    for (int i = 2; i <= size; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of size by multiplying every integer from 2 up to size together, returning 1 for the base case where size is 0 or 1.

int count_vowels_458(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long sum_884(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int gcd_646(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::string reverse_993(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int max_of_525(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

void swap_if_needed_478(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

int clamp_327(int num, int lo, int hi) {
    if (num < lo) return lo;
    if (num > hi) return hi;
    return num;
}
// Explanation: This function constrains num to lie between lo and hi, returning the nearest boundary value if num falls outside that range and num unchanged otherwise.

int max_of_734(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

int stack_peek_or_default_195(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int max_of_786(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

std::string reverse_386(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

long long power_348(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

void swap_if_needed_324(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

int clamp_79(int total, int lo, int hi) {
    if (total < lo) return lo;
    if (total > hi) return hi;
    return total;
}
// Explanation: This function constrains total to lie between lo and hi, returning the nearest boundary value if total falls outside that range and total unchanged otherwise.

int count_words_496(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

bool is_palindrome_477(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

bool is_palindrome_747(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

long long fibonacci_858(int num) {
    long long a = 0, b = 1;
    for (int i = 0; i < num; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the num-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

std::string reverse_494(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

std::vector<std::vector<int>> transpose_643(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long factorial_779(int value) {
    long long result = 1;
    for (int i = 2; i <= value; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of value by multiplying every integer from 2 up to value together, returning 1 for the base case where value is 0 or 1.

int max_of_5(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

long long fibonacci_105(int n) {
    long long a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the n-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int binary_search_236(const std::vector<int>& sorted_items, int total) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == total) return mid;
        else if (sorted_items[mid] < total) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds total by repeatedly halving the search range: comparing total against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int binary_search_861(const std::vector<int>& sorted_items, int amount) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == amount) return mid;
        else if (sorted_items[mid] < amount) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

void swap_if_needed_71(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long sum_447(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int clamp_137(int n, int lo, int hi) {
    if (n < lo) return lo;
    if (n > hi) return hi;
    return n;
}
// Explanation: This function constrains n to lie between lo and hi, returning the nearest boundary value if n falls outside that range and n unchanged otherwise.

int count_words_154(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

bool is_prime_247(int value) {
    if (value < 2) return false;
    for (int i = 2; i * i <= value; i++) {
        if (value % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether value is a prime number by testing for divisors only up to the square root of value, since any larger factor would have a corresponding smaller factor already checked.

double average_368(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

std::vector<std::vector<int>> transpose_583(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long factorial_348(int target) {
    long long result = 1;
    for (int i = 2; i <= target; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of target by multiplying every integer from 2 up to target together, returning 1 for the base case where target is 0 or 1.

long long factorial_307(int count) {
    long long result = 1;
    for (int i = 2; i <= count; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

int count_vowels_983(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long sum_976(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int gcd_408(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::string reverse_87(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

long long power_408(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

long long sum_416(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

bool is_palindrome_860(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int stack_peek_or_default_95(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

double average_201(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

void bubble_sort_991(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

void bubble_sort_105(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

std::vector<std::vector<int>> transpose_898(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

long long fibonacci_847(int data) {
    long long a = 0, b = 1;
    for (int i = 0; i < data; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the data-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int max_of_742(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

long long factorial_394(int total) {
    long long result = 1;
    for (int i = 2; i <= total; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

long long sum_464(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int count_words_259(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

double average_796(const std::vector<int>& values) {
    if (values.empty()) return 0.0;
    long long total = 0;
    for (int x : values) total += x;
    return (double)total / values.size();
}
// Explanation: This function returns the average of the numbers in values, guarding against dividing by zero if values is empty.

std::vector<std::vector<int>> transpose_639(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int count_vowels_920(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int stack_peek_or_default_562(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int binary_search_237(const std::vector<int>& sorted_items, int amount) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == amount) return mid;
        else if (sorted_items[mid] < amount) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds amount by repeatedly halving the search range: comparing amount against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

bool is_prime_743(int num) {
    if (num < 2) return false;
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether num is a prime number by testing for divisors only up to the square root of num, since any larger factor would have a corresponding smaller factor already checked.

int count_words_167(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

long long power_64(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

bool is_prime_968(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

int stack_peek_or_default_332(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

long long fibonacci_468(int target) {
    long long a = 0, b = 1;
    for (int i = 0; i < target; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

long long factorial_94(int count) {
    long long result = 1;
    for (int i = 2; i <= count; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

long long sum_875(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

long long power_123(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int count_vowels_327(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

bool is_prime_589(int count) {
    if (count < 2) return false;
    for (int i = 2; i * i <= count; i++) {
        if (count % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether count is a prime number by testing for divisors only up to the square root of count, since any larger factor would have a corresponding smaller factor already checked.

int linear_search_278(const std::vector<int>& items, int total) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == total) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for total, returning the index of the first match or -1 if total isn't found anywhere in items.

int binary_search_255(const std::vector<int>& sorted_items, int target) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == target) return mid;
        else if (sorted_items[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

void swap_if_needed_109(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

void bubble_sort_297(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

int count_words_933(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int count_words_432(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int clamp_835(int target, int lo, int hi) {
    if (target < lo) return lo;
    if (target > hi) return hi;
    return target;
}
// Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

int count_vowels_617(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

long long power_215(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

bool is_prime_740(int total) {
    if (total < 2) return false;
    for (int i = 2; i * i <= total; i++) {
        if (total % i == 0) return false;
    }
    return true;
}
// Explanation: This function checks whether total is a prime number by testing for divisors only up to the square root of total, since any larger factor would have a corresponding smaller factor already checked.

long long fibonacci_855(int count) {
    long long a = 0, b = 1;
    for (int i = 0; i < count; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the count-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int binary_search_39(const std::vector<int>& sorted_items, int value) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == value) return mid;
        else if (sorted_items[mid] < value) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds value by repeatedly halving the search range: comparing value against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

bool is_palindrome_261(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int gcd_502(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

std::string reverse_709(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

long long factorial_491(int total) {
    long long result = 1;
    for (int i = 2; i <= total; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of total by multiplying every integer from 2 up to total together, returning 1 for the base case where total is 0 or 1.

int binary_search_974(const std::vector<int>& sorted_items, int target) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == target) return mid;
        else if (sorted_items[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int count_vowels_100(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int count_words_852(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int gcd_196(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
// Explanation: This function computes the greatest common divisor of a and b using the Euclidean algorithm: repeatedly replacing the pair with (b, a mod b) until b reaches zero, at which point a holds the answer.

int stack_peek_or_default_915(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int clamp_316(int target, int lo, int hi) {
    if (target < lo) return lo;
    if (target > hi) return hi;
    return target;
}
// Explanation: This function constrains target to lie between lo and hi, returning the nearest boundary value if target falls outside that range and target unchanged otherwise.

int linear_search_365(const std::vector<int>& items, int x) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == x) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for x, returning the index of the first match or -1 if x isn't found anywhere in items.

void swap_if_needed_334(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long power_79(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

int stack_peek_or_default_698(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

int count_words_276(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int linear_search_626(const std::vector<int>& items, int value) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == value) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for value, returning the index of the first match or -1 if value isn't found anywhere in items.

std::string reverse_375(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

int binary_search_490(const std::vector<int>& sorted_items, int num) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == num) return mid;
        else if (sorted_items[mid] < num) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds num by repeatedly halving the search range: comparing num against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

void swap_if_needed_817(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

void swap_if_needed_416(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

long long factorial_73(int x) {
    long long result = 1;
    for (int i = 2; i <= x; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of x by multiplying every integer from 2 up to x together, returning 1 for the base case where x is 0 or 1.

long long factorial_73(int count) {
    long long result = 1;
    for (int i = 2; i <= count; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of count by multiplying every integer from 2 up to count together, returning 1 for the base case where count is 0 or 1.

void bubble_sort_427(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

bool is_palindrome_156(std::string text) {
    std::string cleaned;
    for (char c : text) {
        if (c != ' ') cleaned += std::tolower(c);
    }
    std::string reversed_str = cleaned;
    std::reverse(reversed_str.begin(), reversed_str.end());
    return cleaned == reversed_str;
}
// Explanation: This function checks whether text reads the same forwards and backwards, ignoring case and spaces, by comparing a cleaned-up version of the string against its own reverse.

int max_of_88(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

long long factorial_847(int data) {
    long long result = 1;
    for (int i = 2; i <= data; i++) {
        result *= i;
    }
    return result;
}
// Explanation: This function computes the factorial of data by multiplying every integer from 2 up to data together, returning 1 for the base case where data is 0 or 1.

long long sum_4(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int count_words_478(const std::string& text) {
    std::istringstream stream(text);
    std::string word;
    int count = 0;
    while (stream >> word) count++;
    return count;
}
// Explanation: This function counts the number of whitespace-separated words in text.

int binary_search_509(const std::vector<int>& sorted_items, int target) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == target) return mid;
        else if (sorted_items[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds target by repeatedly halving the search range: comparing target against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int linear_search_460(const std::vector<int>& items, int size) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == size) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for size, returning the index of the first match or -1 if size isn't found anywhere in items.

int count_vowels_749(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int binary_search_846(const std::vector<int>& sorted_items, int x) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == x) return mid;
        else if (sorted_items[mid] < x) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int linear_search_106(const std::vector<int>& items, int data) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == data) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for data, returning the index of the first match or -1 if data isn't found anywhere in items.

std::string reverse_402(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

long long sum_961(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int max_of_260(const std::vector<int>& values) {
    int best = values[0];
    for (size_t i = 1; i < values.size(); i++) {
        if (values[i] > best) best = values[i];
    }
    return best;
}
// Explanation: This function finds the largest value in values by tracking the best value seen so far and updating it whenever a bigger element turns up.

int linear_search_207(const std::vector<int>& items, int count) {
    for (size_t i = 0; i < items.size(); i++) {
        if (items[i] == count) return (int)i;
    }
    return -1;
}
// Explanation: This function searches items one element at a time for count, returning the index of the first match or -1 if count isn't found anywhere in items.

long long fibonacci_75(int target) {
    long long a = 0, b = 1;
    for (int i = 0; i < target; i++) {
        long long temp = a + b;
        a = b;
        b = temp;
    }
    return a;
}
// Explanation: This function returns the target-th Fibonacci number using an iterative approach: it keeps two running values and repeatedly advances them forward, which avoids the exponential blowup of a naive recursive version.

int count_vowels_24(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int stack_peek_or_default_121(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

long long sum_637(const std::vector<int>& values) {
    long long total = 0;
    for (int x : values) {
        total += x;
    }
    return total;
}
// Explanation: This function adds up every element in values and returns the total.

int stack_peek_or_default_205(const std::vector<int>& stack_items, int default_value) {
    if (stack_items.empty()) return default_value;
    return stack_items.back();
}
// Explanation: This function returns the top element of stack_items treated as a stack, or default_value if stack_items is empty, so callers don't have to check emptiness themselves before peeking.

long long power_909(int base, int exponent) {
    long long result = 1;
    for (int i = 0; i < exponent; i++) {
        result *= base;
    }
    return result;
}
// Explanation: This function raises base to the power of exponent by multiplying base into an accumulator exponent times.

void swap_if_needed_786(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

std::vector<std::vector<int>> transpose_505(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

void swap_if_needed_190(int& a, int& b) {
    if (a > b) {
        std::swap(a, b);
    }
}
// Explanation: This function swaps a and b only if a is currently greater than b, so the pair ends up in ascending order.

int binary_search_853(const std::vector<int>& sorted_items, int num) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == num) return mid;
        else if (sorted_items[mid] < num) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds num by repeatedly halving the search range: comparing num against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

int count_vowels_83(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.

int binary_search_346(const std::vector<int>& sorted_items, int x) {
    int lo = 0, hi = (int)sorted_items.size() - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (sorted_items[mid] == x) return mid;
        else if (sorted_items[mid] < x) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
// Explanation: This function assumes sorted_items is already sorted and finds x by repeatedly halving the search range: comparing x against the middle element and discarding the half that can't contain it, which is why it's much faster than a linear scan on large sorted data.

std::string reverse_408(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

std::vector<std::vector<int>> transpose_507(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

void bubble_sort_485(std::vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
}
// Explanation: This function sorts arr in ascending order using bubble sort: it repeatedly walks through the list swapping adjacent out-of-order elements, so after each full pass the largest remaining unsorted element bubbles into place at the end.

std::string reverse_119(std::string text) {
    std::reverse(text.begin(), text.end());
    return text;
}
// Explanation: This function returns text with its characters in reverse order.

std::vector<std::vector<int>> transpose_771(const std::vector<std::vector<int>>& matrix) {
    int rows = matrix.size();
    int cols = matrix[0].size();
    std::vector<std::vector<int>> result(cols, std::vector<int>(rows));
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            result[j][i] = matrix[i][j];
        }
    }
    return result;
}
// Explanation: This function returns the transpose of matrix, flipping it over its diagonal so that rows become columns and columns become rows.

int count_vowels_160(const std::string& text) {
    std::string vowels = "aeiouAEIOU";
    int count = 0;
    for (char c : text) {
        if (vowels.find(c) != std::string::npos) count++;
    }
    return count;
}
// Explanation: This function counts how many characters in text are vowels, checking each character against a fixed list of vowel letters in both cases.
